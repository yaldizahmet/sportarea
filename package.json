{
  "name": "sporarea",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix mobile",
    "server": "npm start --prefix server",
    "mobile": "npm start --prefix mobile",
    "mobile:lan": "npm run start:lan --prefix mobile",
    "mobile:tunnel": "npm run start:tunnel --prefix mobile",
    "mobile:web": "npm run web --prefix mobile",
    "dev:web": "concurrently -n server,expo -c blue,magenta \"npm run server\" \"npm run web --prefix mobile\"",
    "dev:phone": "concurrently -n server,expo -c blue,green \"npm run server\" \"npm run start:lan --prefix mobile\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
